server:
  port: 8501

spring:
  redis:
    host: localhost
    timeout: 1000ms
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
  cache:
    type: redis

logging:
  level:
    com.sixkery: debug
